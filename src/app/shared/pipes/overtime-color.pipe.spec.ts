import { OvertimeColorPipe } from './overtime-color.pipe';

describe('OvertimeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new OvertimeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
